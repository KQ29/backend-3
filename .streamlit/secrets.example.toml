# Streamlit Community Cloud root-level secrets.
# BACKEND_API_TOKEN must match the token configured on the FastAPI host.
STREAMLIT_API_URL = "https://your-fastapi-service.example.com"
BACKEND_API_TOKEN = "replace-with-a-long-random-shared-token"
LLM_TIMEOUT_SECONDS = "30"
STT_TIMEOUT_SECONDS = "60"
STREAMLIT_API_TIMEOUT_SECONDS = "120"
STREAMLIT_VOICE_TIMEOUT_SECONDS = "210"
